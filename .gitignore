# Built deployment artifacts (rebuilt by make-zips.ps1) — may contain live API keys, never commit
*.zip
*.tar.gz

# SERVER-SIDE API KEYS — never commit. Only keys.example.php belongs in git.
keys.php
*/keys.php

# Local tooling state
.gstack/
.claude/settings.local.json

# Node
node_modules/
