# Design System — 7By.in

## Product Context
- **What this is:** Free AI-powered audio and media processing tools platform
- **Who it's for:** Musicians, podcasters, content creators, developers, and everyday users who need quick audio/image processing without paid software
- **Space/industry:** Online tools / AI SaaS — peers include remove.bg, vocalremover.org, noise.ai
- **Project type:** Web app with marketing landing page + blog
- **Founder/CEO:** Shirandasu Sandeep

## Aesthetic Direction
- **Direction:** Dark Glassmorphism + Neon Waveform
- **Decoration level:** Expressive — animated waveform hero, glowing card halos, grain texture on glass surfaces
- **Mood:** Premium and powerful. The tools should feel like studio-grade software running in a browser, not a free web widget. Dark-only — the aesthetic IS the brand.
- **Memorable thing:** "The dark tools site where everything glows when you touch it."

## Typography
- **Display/Hero:** Outfit 800 — ultra-bold, tight tracking (-3px at large sizes), strong authority
- **Body:** Outfit 400/500 — same family reduces load overhead, excellent legibility at 14-18px
- **UI/Labels:** JetBrains Mono — signals precision/tech, used for tags, badges, tool metadata, format labels
- **Data/Tables:** JetBrains Mono (tabular-nums) — consistent column widths for audio metadata
- **Code:** JetBrains Mono
- **Loading:** Google Fonts CDN — `family=Outfit:wght@300;400;500;600;700;800&family=JetBrains+Mono:wght@400;500`
- **Scale:**
  - Hero: 96px / 800 / -3px tracking / 1.0 line-height
  - H1: 52px / 800 / -1.5px
  - H2: 32px / 800 / -1px
  - H3: 22px / 700 / -0.5px
  - Body: 16px / 400 / 1.6
  - Small: 14px / 400
  - Mono label: 11-13px / 400-500 / +2-3px tracking / uppercase

## Color
- **Approach:** Expressive — color as primary design tool, neon on dark
- **Background:** `#09090F` — deep slate, almost black with a slight blue shift
- **Surface:** `#0F0F1A` — card and panel backgrounds
- **Surface 2:** `#141428` — nested surfaces, waveform demo backgrounds
- **Primary (Cyan):** `#00D4FF` — audio tools, primary CTAs, active states, progress bars
- **Secondary (Magenta):** `#FF006E` — video tools, secondary actions, accent highlights
- **Tertiary (Amber):** `#FFB800` — image tools, AdSense slot indicators, warning states
- **Text Primary:** `#F0F0F8` — main content
- **Text Muted:** `rgba(240,240,248,0.5)` — secondary text, descriptions
- **Text Dim:** `rgba(240,240,248,0.3)` — timestamps, metadata, placeholders
- **Border:** `rgba(255,255,255,0.08)` — default
- **Border Bright:** `rgba(255,255,255,0.15)` — hover states
- **Semantic:** success `#00C896`, warning `#FFB800`, error `#FF4444`, info `#00D4FF`
- **Tool category glow system:**
  - Audio tools: `box-shadow: 0 0 0 1px rgba(0,212,255,0.3), 0 16px 40px rgba(0,212,255,0.12)`
  - Video tools: `box-shadow: 0 0 0 1px rgba(255,0,110,0.3), 0 16px 40px rgba(255,0,110,0.12)`
  - Image tools: `box-shadow: 0 0 0 1px rgba(255,184,0,0.3), 0 16px 40px rgba(255,184,0,0.12)`
- **Dark mode:** Dark-only. No light mode toggle. The dark aesthetic is the brand identity.

## Spacing
- **Base unit:** 8px
- **Density:** Comfortable — enough whitespace to breathe, enough density for AdSense slots
- **Scale:** 2xs(4) xs(8) sm(12) md(16) lg(24) xl(32) 2xl(48) 3xl(64) 4xl(80)
- **Section padding:** 80px vertical, 32px horizontal (mobile: 60px / 16px)

## Layout
- **Approach:** Hybrid — 12-column strict grid for tool pages (AdSense placement friendly), creative editorial for hero and blog
- **Grid:** 12 columns, 24px gutter
- **Max content width:** 1280px
- **Nav height:** 64px fixed
- **Border radius:** sm(6px) md(10px) lg(12px) xl(20px) pill(9999px)
- **Tool cards:** `minmax(300px, 1fr)` auto-fill grid, 20px gap
- **Blog cards:** `minmax(300px, 1fr)` auto-fill grid, 24px gap

## Google AdSense — Placement Strategy
Four ad slots per page for maximum revenue while preserving UX:

1. **Below-hero Leaderboard (728×90)** — highest CTR position, above the fold after the hero CTA
2. **Tool page right sidebar Rectangle (300×250)** — standard "golden" AdSense unit, appears while user waits for processing
3. **In-content Leaderboard (728×90)** — between tools section and blog section
4. **Pre-footer Leaderboard (728×90)** — catches users scrolling to the bottom

AdSense approval checklist met by design:
- Sufficient original content (tool descriptions + blog)
- Clear navigation and site structure
- Privacy policy and terms of service pages linked in footer
- No deceptive ad placement (dashed border + label on placeholder slots)
- Mobile responsive layout (ads resize correctly)

## Motion
- **Approach:** Expressive — scroll-driven entrance animations, waveform hero, glowing pulse
- **Easing:** enter(`ease`) exit(`ease-in`) move(`ease-in-out`)
- **Duration:** micro(100ms) short(200ms) medium(300ms) long(500ms)
- **Key animations:**
  - Hero waveform: canvas-based, mouse-reactive, 4 layered sine waves in cyan/magenta
  - Tool cards: `fadeUp` on scroll intersection (opacity 0→1, translateY 24px→0, 500ms)
  - Card hover: `translateY(-4px)` + category glow (300ms)
  - Nav CTA hover: `translateY(-1px)` (200ms)
  - Waveform bars: randomized height static bars, color-coded by stem type
  - Dot badge: `pulse` keyframe (scale + opacity, 2s infinite)

## Tool Category Color System
| Category | Color | Hex | Dim (backgrounds) | Use |
|----------|-------|-----|-------------------|-----|
| Audio | Cyan | `#00D4FF` | `rgba(0,212,255,0.15)` | Vocal remover, noise remover, pitch shifter, converter |
| Video | Magenta | `#FF006E` | `rgba(255,0,110,0.15)` | Video tools (future) |
| Image | Amber | `#FFB800` | `rgba(255,184,0,0.15)` | BG remover, image enhancer |

## Component Patterns
- **Nav:** Fixed, `backdrop-filter: blur(20px)`, dark glass, 64px height
- **Hero badge:** Mono font, cyan border, animated pulse dot, pill shape
- **Tool cards:** Glass surface + border, category icon in tinted square, category label in mono, glow on hover
- **Upload area:** Dashed cyan border, hover intensifies, format badges in mono
- **Waveform bars:** Flex row, randomized heights, color by stem (white/cyan/magenta)
- **Play buttons:** Circle, filled with category color, dark text/icon
- **Progress bar:** 4px track, filled with category color
- **Blog cards:** Glass surface, colored gradient thumbnail by category, avatar initials circle with cyan-magenta gradient
- **Footer:** 4-column grid, logo + descriptor, link columns, mono copyright

## Decisions Log
| Date | Decision | Rationale |
|------|----------|-----------|
| 2026-06-29 | Dark-only, no light mode toggle | Brand IS the dark aesthetic; light mode would dilute identity and reduce ad contrast |
| 2026-06-29 | Outfit font family for both display and body | Single CDN request, strong personality at 800 weight, clean at 400 |
| 2026-06-29 | Three-color category coding (cyan/magenta/amber) | Gives each tool type a distinct glow identity without needing icons alone |
| 2026-06-29 | Mouse-reactive waveform hero | Memorable first impression — the site responds to you before you've done anything |
| 2026-06-29 | 4 AdSense placements | Maximizes revenue while keeping tool pages functional; sidebar placement captures waiting users |
| 2026-06-29 | Initial design system created | Created by /design-consultation for 7By.in — Shirandasu Sandeep |
| 2026-07-06 | VidLab video suite (`videotools/`) uses a light theme | Explicit user request/approval; keeps brand fonts (Outfit + JetBrains Mono) and the magenta video-category accent (#FF006E) so it reads as a 7By product. Light theme is scoped to VidLab only — main site stays dark-only |
| 2026-07-10 | PriceHunt switched to a light theme | Explicit user request ("keep lite theme light colour theme"). Keeps brand fonts (Outfit + JetBrains Mono); accents darkened for light-bg contrast: cyan #0891B2, magenta #DB2777, amber #D97706, green #059669 for "lowest price". Scoped to PriceHunt only — main site stays dark-only |
| 2026-07-08 | PriceHunt affiliate price-comparison site (`pricehunt/`) created on the dark system | Superseded 2026-07-10 by light theme (above) |
| 2026-07-07 | VoxTwin voice-clone studio (`voiceclone/`) uses a light theme | Explicit user request ("lite colour theme"). Keeps brand fonts (Outfit + JetBrains Mono); accent is a cyan→violet gradient (#06B6D4→#7C3AED) derived from the audio-category cyan, darkened for light-background contrast. Scoped to VoxTwin only — main site stays dark-only |
| 2026-07-10 | 7Pay payment gateway (`pay/`) uses a light theme | Explicit user request ("using light theme"). Keeps brand fonts (Outfit + JetBrains Mono); accent is indigo #4F46E5 — 7By's existing payments color (OTP emails, Razorpay checkout theme). Checkout header uses an indigo→violet gradient (#4F46E5→#6D28D9). Light theme signals trust/clarity for a payments surface; scoped to 7Pay only — main site stays dark-only |
| 2026-07-16 | 7By Writer AI writing suite (`writer/`) uses a light theme | Explicit user request ("convert it into lite theme"). Keeps brand fonts (Outfit + JetBrains Mono); accents darkened for light-bg contrast per the PriceHunt/Facet convention: cyan #0891B2, magenta #DB2777, amber #D97706, green #059669; text #101425 on bg #F7F8FC. Category glow system preserved as soft tinted shadows. Scoped to 7By Writer only — main site stays dark-only |
| 2026-07-30 | 7Leaf switched to a light theme | Explicit user request ("keep in light theme no dark theme"). Supersedes the dark-system entry below. Follows the established per-tool light convention (PriceHunt / VoxTwin / 7Pay / Writer / Facet): brand fonts kept, accents darkened for contrast on `#F7F8FC` — green `#047857` for text and button fills, `#059669` for gradients and tints that carry no text, cyan `#0891B2`, amber `#D97706`, magenta `#DB2777`, red `#DC2626`, text `#101425`. Two greens rather than one because a single `#059669` measured 3.55:1 against the background and failed for 11px mono labels and button captions; the split gets both to 5.1:1+. `--text-dim` also darkened to `#6B7280` (4.55:1) after the original grey measured 2.65:1 while carrying real content. Glass surfaces replaced with `--shadow-sm/md/lg`; the category "glow" system survives as soft tinted shadows on the result card. Scoped to 7Leaf — main site stays dark-only |
| 2026-07-30 | 7Leaf clinic surfaces (triage, treatment plan, case history, paywall) | Triage borrows the severity palette rather than adding colours: red = act today, amber = this week, cyan = monitor, green = clear, on a left-hand dot plus the heading. The treatment plan is one interleaved list grouped Today / This week / Keep doing — not one list per disease, because a plant with two problems still only has one afternoon. Trend sparkline colours only the *latest* bar, so the eye lands on where the plant is now rather than reading the whole series as a gradient. Paywall keeps the free column visually equal to the Pro column (same card, same type scale) since the free tier is the product, not a teaser |
| 2026-07-29 | 7Leaf plant health checker (`plant/`) built on the standard dark system — superseded 2026-07-30 | No light-theme request, so it keeps the dark-only brand identity. Accent is the existing semantic green `#00C896` — the plant category colour — used for CTAs, the hero badge and the score ring; the cyan/amber/magenta system colours are reserved for severity (watch / act this week / act today) so the finding cards read at a glance. Health-map overlay colours (green / lime / amber / orange-brown / red / off-white / magenta) are data encodings for tissue classes, not decoration, and are listed in the legend with their percentages |
| 2026-07-29 | 7Leaf flower-sex and optional-AI surfaces | Sex uses the existing category colours as a semantic set — magenta `#FF006E` for ♀, cyan `#00D4FF` for ♂, green `#00C896` for hermaphrodite (the common case) — carried on the card's left border and symbol chip only, so it reads without adding a fourth accent to the palette. The optional AI panel is the one component with a **dashed** border (`--border-bright`): everything measured locally has a solid border, so "solid = your device, dashed = leaves your device" is a visual rule the user can learn. Hero badge changed from "NO AI · NO UPLOAD" to "MEASURED, NOT GUESSED · RUNS IN YOUR BROWSER" once the optional AI layer existed — the old claim would have been false |
| 2026-07-14 | Facet desktop-widgets site (`widgets/`) uses a light theme | Explicit user request ("give me lite colour theme"). New product: showcase + real download site for themeable desktop widgets. Keeps brand fonts (Outfit + JetBrains Mono); accents darkened for light-bg contrast: cyan #0891B2, violet #7C3AED (cyan→violet brand gradient on CTAs/logo), magenta #DB2777, green #059669. Widget "materials" (glass/wood/frost/neon/aurora/carbon/gold/paper) carry their own scoped CSS-var palettes that theme the widget chrome only, not the site — so the site chrome is light while widgets sit on dark material wallpapers. Scoped to Facet only — main site stays dark-only. Ships as an installable PWA (manifest + service worker → `app.html`) plus a client-side standalone-widget `.html` download builder |
| 2026-08-01 | 7Pet desktop companion (`pet/`) built on the standard dark system, with two deliberate deviations | Electron desktop app, not a web surface. **No Google Fonts**: 7Pet's entire pitch is that nothing about your day leaves the machine, and a CDN webfont is a network request, so the brand stack falls back to local equivalents (`Segoe UI Variable Display` for Outfit, `Cascadia Mono` for JetBrains Mono). **No binary image assets**: the app/tray icon is rasterised in pure Node by `assets/icon-encoder.js` (hand-rolled PNG + ICO encoder), so the creature's silhouette and brand colour live in diffable source rather than a .ico nobody can edit. Dashboard is the standard dark system unchanged — `#09090F` bg, cyan `#00D4FF` focus, magenta `#FF006E` drain, green `#00C896` breaks, amber `#FFB800` streak |
| 2026-08-01 | 7Pet creature: mood colour outranks life-stage colour | The creature's body tints by mood (cyan content, indigo sleepy, grey sad, olive queasy, orange frazzled) and an Elder additionally warms to gold. Those collided: `.stage-elder` and `.mood-*` are equal specificity, so the later gold rule won and a sad Elder looked identical to a proud one — destroying the one thing the product exists to do, which is let you read your day from across the room. Gold is now scoped by two-class selectors that list the healthy moods explicitly (`.stage-elder.mood-content`, `.mood-happy`, `.mood-proud`, `.mood-egg`), which outrank the single-class mood rules; every distress mood keeps its own colour at every stage. Rule: stage is identity, mood is signal, signal always wins |
| 2026-08-01 | 7Pet landing page (`pet/site/`) stays on the dark system, and its hero is a live creature rather than a screenshot | No light-theme request, and the app itself is dark, so the download site keeps the brand identity — unlike PriceHunt/VoxTwin/7Pay/Writer/Facet/7Leaf, which all switched on explicit request. Fonts come from the Google Fonts CDN here (the no-CDN rule is scoped to the offline desktop app, where a webfont request would break the no-network promise; a website has no such promise to break). The hero and the "push it through a day" demo run the **real** creature SVG with the real `mood-*`/`stage-*` class names and a small copy of the economy, so pressing "an hour of Reels" turns it queasy on the page. Reasoning: the product is a creature, and no screenshot can sell a creature — the visitor has to meet it. Gradient ids are scoped per instance (`b1`…`b7`) because duplicate SVG ids all resolve to the first in the document, which would have painted all seven creatures in the hero's colour |
| 2026-08-01 | 7Pet app-time bars are layered, not segmented | Drain is classified from the window title ("Instagram — Google Chrome") but only the process name is ever persisted, so a single "chrome" row would have shown one honest-looking cyan bar hiding an hour of Reels. A parallel `drainApps` tally is kept per day, and each row draws total time in cyan with the drain portion overlaid in magenta from the same left edge, plus a split label (`1h 35m · 45m drain`). A mostly-magenta row reads as mostly wasted with no arithmetic |
| 2026-07-29 | 7Compare Telegram channel CTA (`7compare/`) stays on the site palette | Explicit user request ("add a telegram channel button in our site hightlight it"). Deliberately NOT Telegram brand blue (#229ED9) as the button colour — that would be a palette deviation. The CTA reuses the existing `.unique-cta` pill shape on the site's cyan (#0E7490→#0891B2→#06B6D4), and recognition is carried by the official Telegram paper-plane mark, which keeps its blue inside a white circular badge. Shine animation delayed 1.6s so the two hero pills don't flash in unison. Placed in hero (next to Unique Finds), nav (pill, >1024px only — 641-1024px wraps the six-link nav onto two rows, so it is hidden there and reverts to a plain menu row inside the ≤640px dropdown) and footer. Channel: t.me/compare7by |
| 2026-08-03 | 7Forge image-to-3D studio (`forge/`) built on the standard dark system, unchanged | No light-theme request, and the product is a 3D creation tool where a dark viewport is the professional convention (Blender, Meshy, Mixamo all ship dark) — so the brand dark system needed no deviation. Cyan `#00D4FF` is the primary and carries the wireframe overlay and grid; magenta `#FF006E` is reserved for the **bounding box** and the progress-bar gradient terminus, so the two accents never compete inside the viewport. A new semantic set was added for *verification state* rather than tool category, because this product's core promise is honesty about what has actually been proven: green `#00C896` = verified on real hardware, amber `#FFB800` = implemented but never run, magenta `#FF006E` = experimental. These render as a badge next to every engine. The viewport background is a user choice (dark / grey / light / transparent) rather than a brand constant — judging a generated mesh needs a neutral backdrop, and forcing `#09090F` behind a dark model would hide exactly the geometry the user is inspecting. First 7By product using React + TypeScript + Vite rather than vanilla HTML/PHP; justified by a real-time 3D viewport plus WebSocket progress, which is beyond what the vanilla stack does well. Fonts stay Outfit + JetBrains Mono from the Google CDN (it is a website, so the offline no-CDN rule from 7Pet/DesktopVerse does not apply) |
| 2026-08-06 | 7Sheet AI spreadsheet platform (`sheet/`) built on the standard dark system, unchanged | No light-theme request, and a spreadsheet workspace is a surface people stare at for hours — the dark system is the right call and needed no deviation. Cyan `#00D4FF` is the single brand accent (CTAs, active cells, KPI values, the primary chart series); the other system colours are used **semantically, not decoratively**: green `#00C896` = positive/verified, magenta `#FF006E` = loss/overdue, amber `#FFB800` = needs attention, red `#FF4444` = error. That mapping is also what conditional formatting emits, so a red cell in the grid means the same thing as a red badge in the UI. Charts use the five accents as a **categorical** set only — never as a gradient over values — with axis labels in JetBrains Mono tabular-nums so columns line up. The grid itself deliberately breaks one house rule: cell borders are `rgba(255,255,255,0.05)` rather than `--border` (0.08), because at 28px row height the standard border reads as a heavy mesh and the data stops being scannable. Charts, KPI cards and the sheet grid are all hand-drawn SVG/CSS rather than a charting library, so the palette cannot drift. First 7By product to ship its own spreadsheet grid; the mobile layout replaces the sidebar with the standard 5-item bottom nav and moves the AI assistant to a full-screen sheet that starts **closed** — opening a file on a phone should show the file |
| 2026-08-06 | 7Solve (`7solve/`) rebuilt as a physical spiral notebook; accent moved from emerald/teal to **blue ink** | Explicit user request, with a reference image: the app should read as "the world's most beautiful interactive study notebook", continuing seamlessly out of the 10s notebook preloader rather than looking like a SaaS dashboard. The palette is now the one a real student's desk has — cream paper `#fdfaf0`, blue ink `#2647cf` (primary, and the only accent that carries interaction), black handwriting `#15203a`, teacher red `#c2352b` (errors, delete, the bookmark on a chosen card), sticky yellow `#fbe98c` (hints, toasts), soft green `#2f7d5a` (correct/verified), wood browns for the desk behind the page. This **replaces** the emerald/teal accent 7Solve shipped with; teal survives nowhere in the app. Rationale for blue over teal: the whole surface is now cream paper with blue ruled lines, and teal on cream reads as a brand colour applied to a notebook, whereas blue reads as ink *on* the notebook — the accent had to be the same substance as the rules and the handwriting or the metaphor breaks. The legacy CSS variable names (`--maroon`, `--gold`, `--leather`, `--sel`) were kept and **remapped** rather than renamed, so every one of the app's existing rules kept working through the reskin with no behavioural risk. `--ink-faint` is `#606a82`, chosen by measurement: the first pick `#6e7692` measured 4.18:1 on cream and it carries real content (form labels, the class list, saved-doubt dates), so it was darkened until it cleared 4.5:1 on **both** the page and the nested `--paper-2` card (5.2:1 / 4.8:1). **Typography left the brand serif entirely** — Playfair Display + Lora were the right voice for a *book* and the wrong one for a *notebook*, so the whole stack is now handwritten: **Baloo 2** (600–800) is the chunky marker for the masthead, section headings and every small-caps label; **Patrick Hand** is the upright hand-print for everything you actually read; **Caveat** stays for the loose hand — sticky notes, margin doodles, input placeholders, the signature. Patrick Hand ships no bold, which is *why* Baloo 2 carries all the weight rather than leaving the browser to synthesise it. Hindi and Telugu fall through to **Noto Sans** Devanagari/Telugu (swapped from Noto Serif to sit with the hand-print rather than fight it); Baloo 2 covers Devanagari natively so headings stay on-voice in Hindi. Hand-print measures smaller than a serif at the same px, so every size came up 1.5–2.5px and line-height came down. Equations are the one deliberate exception: `.nb-formula` and inline code are **monospace**, because a handwriting font that renders `1` like `l` is a liability in a product students revise arithmetic from. Accepted trade-off: a decorative face across the whole UI is harder to read than a workhorse sans for some users — the reference and the brief both call for it explicitly, and Patrick Hand is among the most legible hand-prints available, but a plain-text reading toggle is the obvious escape hatch if that ever bites. **A ruled line never strikes through writing.** The rules run the sheet's full height on a fixed 34px pitch while text flows freely, so lines were cutting straight through headings and labels. Rather than force the whole app onto a baseline grid (fragile — it drifts the moment any element has a non-multiple height), anything that sits directly on the paper with no surface of its own now carries a **paper band**: `.chapter`, `.hint`, `.field label`, `.cg-label`, `footer`, and the whole header/toolbar block. The rules stop at the writing instead of crossing it, which is what the reference does — its rules begin below the tab row, not behind the masthead. This required flattening the sheet's base from a horizontal gradient to a single flat cream, otherwise a band would show a seam against the gradient. **Coverage is the product**: 465 courses and exams across the seven levels — 302 of them competitive exams spanning UPSC, all 28 state PSCs plus every UT (Assam, Arunachal, Manipur, Meghalaya, Mizoram, Nagaland, Sikkim, Tripura, Goa, J&K, Ladakh, Puducherry, A&N, Chandigarh, Delhi), 13 state subordinate-service boards, 20 state TETs, 18 police forces, and the village-level posts a first-generation aspirant actually sits (VRO/VRA, Patwari, Panchayat Secretary, Anganwadi, Forest Guard, India Post GDS) — plus 154 course-specific subject sets so a Class 3 student sees EVS and drawing while a B.Tech CSE student sees Compiler Design, and nobody scrolls past a subject that is not on their syllabus. **Course and subject pickers are a searchable notebook index**, not a `<select>` — 152 exams cannot be browsed in a native dropdown. The `<select>` stays in the DOM as the single source of truth (every existing caller of `.value` and every `change` listener was left untouched); it is only replaced on screen. The panel rebuilds from the live options on each open, so it cannot drift from what `refreshSubjects()` just wrote, and matches are marked with the highlighter yellow. **The page turns once per course**: choosing a level flips the sheet and lands on Ask your doubt, but only the first time that level is picked in a session — repeat picks and the straight-to-camera path never see it, because an animation you cannot avoid stops being delightful on the second viewing. Every control is a paper object — nav and language switches are notebook tabs that lift and draw a pen underline on hover, the chosen study level drops a red bookmark and takes a teacher's tick, the photo upload is taped and paper-clipped, the primary button is a blue teacher's stamp that spreads ink when pressed, the answer is a ruled page with a red margin and a lifting corner, and saved doubts are colour-spined notebooks on a shelf. Decorative layers (desk, spiral, paper clips, sticky notes, page curl, margin doodles) are **all** `pointer-events:none` — a decoration that can intercept a tap is a bug, not a flourish. Margin doodles (triangle, E=mc², parabola, paper plane, molecule, DNA, flask, circuit, binary/flowchart, star, heart, "?") only appear ≥1180px, where the page grows real 116/132px gutters for them; below that they are removed rather than allowed to sit behind the text. **Answers carry 15 required sections** (Answer+Difficulty, Definition, Concepts & Formulas, Step-by-Step, Alternative Method, Important Notes, Common Mistakes, Quick Tip, Memory Trick, Real-Life Example, Teacher's Remark, Practice Question, Summary, Revision Box, and a Diagram on request); 12 of them render as their own tinted study card, while Concepts and Step-by-Step stay as body text because they *are* the answer, not a callout. Card tints extend the existing grammar rather than inventing one — blue for reference (Definition, Revision), teal for the alternative route, amber for notes and practice, pink for the memory trick, green for real life, red-on-a-tilt for the teacher's remark, grey for the summary. Each section is capped at 1–3 lines with an explicit instruction to say "this does not apply" rather than fabricate, because forcing an Alternative Method onto a question that has none is an accuracy risk on a product students revise from |
| 2026-08-08 | InviteVerse AI (`inviteverse/`) ships its own identity, **"Midnight Atelier"**, and is the first 7By product whose guest-facing surface has no fixed palette at all | Explicit user request: "Create a fresh visual identity… Do NOT copy Apple, Canva, Evite, Greetings Island." The brief is an AI-first invitation *generator*, so the product splits into two surfaces with different rules. **The chrome** (marketing, prompt studio, dashboard, editor) is Midnight Atelier: ground `#0A0616` deep aubergine-black, surfaces `#120A26`/`#1A1033`, foil gold `#E8C577` as the primary and the only accent that carries interaction, rose `#FF8FB1` for emotional actions (RSVP yes, send a wish), dusk violet `#8B6BFF` for AI surfaces, mint `#59E1D4` for live/success. Deliberately not the 7By cyan/magenta-on-slate system: that reads as studio software, and this is a room before a ceremony. Type left the 7By stack entirely — **Cormorant Garamond** 300 for display (a high-contrast serif is the invitation voice; Outfit 800 is a product voice), **Manrope** for UI, **Italiana** for the ceremonial all-caps eyebrow that sits above every section the way a printed card uses a rule and a word. Signature treatments: `foil-text` (an animated gold-leaf gradient), `foil-rule` (a hairline that blooms at the centre), and an entrance that unmasks upward with a blur-to-focus rather than a fade — "never a plain fade" is enforced by there being no fade utility. **The guest invitation has no brand palette**: it renders entirely from a generated `InvitationSpec`, so the colours, fonts, radius, density, layout and motion are different on every invitation. Three consequences worth recording. (1) **Contrast is enforced by measurement, not by taste** — `enforceContrast` runs over every palette, generated locally or returned by a model, before it can be stored: body text to 4.5:1, accents to 3.0:1 (the AA large-text bar; holding a gold accent to 4.5:1 on cream forces it brown and destroys the look for no gain at display sizes). Adjustments are surfaced to the host, never made silently. (2) **Fonts are invented pairings of real families** — a model-hallucinated family 404s on Google Fonts and the invitation silently loses its typography, so unknown names snap to the nearest catalogue member in the right voice; the *combination* is novel, the files exist. (3) **An opening object is one material** — the golden gate originally drew its bars in `accent2` and its arcs in `accent`, which reads as gold-and-green scaffolding whenever the generated harmony is complementary rather than analogous; structure is now all one accent with depth from opacity and stroke weight, which is stable across any palette. Light mode exists but is scoped to the dashboard/admin chrome only, resolved before first paint by a path check in the inline script — the marketing site is dark-only because the palette *is* the brand, and an invitation already renders in its own theme's colours, so a second competing appearance setting there would fight the theme. One cascade-layer note for anyone extending it: the custom classes live in `@layer components`, because Tailwind v4 emits utilities into `@layer utilities` and *unlayered* CSS beats any layered rule regardless of specificity — a bare `.btn { display:inline-flex }` silently defeated `lg:hidden` and every `!p-0` override until the layers were added |
| 2026-08-02 | DesktopVerse AI desktop companion (`desktopverse/`) built on the standard dark system, with three deliberate deviations | .NET 9 WPF/Direct2D Windows app, not a web surface. **A light theme ships alongside dark** — the product spec explicitly requires both, and unlike PriceHunt/VoxTwin/7Pay/Writer/Facet/7Leaf (which each *switched*) this one keeps dark as the default and the brand while offering light because the settings window sits on the user's own desktop and should be able to match their system chrome. Light accents follow the established darkened set: cyan `#0891B2`, magenta `#DB2777`, amber `#D97706`, green `#059669`, text `#101425` on `#F7F8FC`. **No Google Fonts CDN** — same rule 7Pet established for offline desktop apps; Outfit and JetBrains Mono ship as embedded WPF resource fonts with `Segoe UI Variable Display` / `Cascadia Mono` as the fallback chain. **Glassmorphism is delivered by Mica** (`DwmSetWindowAttribute(DWMWA_SYSTEMBACKDROP_TYPE)`) rather than a `backdrop-filter` analogue — it is real, GPU-composited and wallpaper-aware on Windows 11; Windows 10 has no Mica and falls back to flat `#0F0F1A` with the existing border and glow tokens, so the brand look survives on both. Web type scale is reduced for a 1040×700 window (hero 96px → page title 32px) with ratios and the 8px spacing unit preserved. Full spec in `desktopverse/docs/UX-SPEC.md` |
| 2026-08-12 | 7Solve rebuilt as a full product on the **light premium** system, from three reference boards | Explicit user request with three images: board 1 is the preloader, board 2 is the app theme, board 3 is the icon. This supersedes the 2026-08-06 spiral-notebook entry — the cream paper, the ruled lines, the handwritten Baloo 2 / Patrick Hand / Caveat stack and every paper object are gone. The surface is now white cards on `#f4f6fd`, hairline `#e6e9f7` borders, very low very wide blue-shifted shadows, and one brand gradient `#3b7bfb → #5b4df0 → #9540ee` that is the only thing in the product allowed to be saturated. **The icon is fixed and is never redrawn**: a rounded-square keyline broken at the top right where a bold tick escapes it, a chunky gradient 7, the tick knocked out of the 7 in white so the two shapes stay separable at 16px, and two four-point sparkles. It lives as one `<symbol id="logo7">` used by the preloader, sidebar, hero, footer, empty states, PDF export and `assets/logo.svg` (favicon + PWA), so there is exactly one copy to change and no drift. A second symbol, `bot7`, is the mascot from boards 1 and 2 — drawn as SVG rather than shipped as a raster so it costs nothing and inherits the gradient. **The header is English-only**, as the brief demands: the EN/हिं/తెలు tab group was removed from the top bar and language moved into Settings. The i18n layer itself was left entirely intact — `applyLang()`, all three `I18N` tables and every `data-i18n` binding still work, and a new `setLang()` plus a delegated `.lang-tab` listener drive it from wherever it is rendered — so adding a language is still a data change, not a rebuild. **`window.state` had to be exported.** The app script declares `state`, `LEVELS`, `LANG_NAME`, `I18N` and `COURSE_SUBJECTS` with `let`/`const` at the top level of a classic script, which puts them in script scope, *not* on `window` — so every new module read `undefined`, and a pre-existing guard (`window.quickFollow && window.state && ...`) had been silently dead. Five explicit exports fix both; `state` is only ever mutated in place, never reassigned, so the shared reference holds. **Navigation became a view router.** The dashboard is now one of sixteen sibling `.view` sections that the router swaps, with hash deep-links (`#tutor`, `#flashcards`), back-button support, and a `Views` registry so each feature draws itself on mount. Home keeps its scrolling anchors (`#ask`, `#answer`, `#notebook`) because those are places inside one page, not separate pages. **The header tabs only fit on very wide screens** and that is by measurement, not taste: at 1280px the sidebar and rail leave the working column 643px, and search + five tabs + Pro + bell + profile need ~795px. Tabs go icon-only below 1720 and disappear below 1400, where the sidebar is on screen and carries every one of the same destinations, so nothing becomes unreachable. The engine chip left the header entirely — it is diagnostic, not navigation, and Settings surfaces it instead. **Each semantic colour now ships in two weights**, the split 7Leaf established: the bright value (`--ok` `#0f9d63`, `--warn` `#c47a08`, `--err` `#e0396b`) fills bars, rings, dots and borders where nothing is read; the `-ink` value (`#0b6b45`, `#8a6413`, `#a52347`) is the only one allowed to carry text on its own tint. Measured: green-on-green-tint was 3.16:1 and failed, the ink is 5.95:1. `--ink-3` also came down from `#6b7397` to `#656d90` so the 10–11px labels clear 4.5:1 on the nested `--surface-2` card as well as on white, and the footer's "AI-generated — please verify before your exam" line moved off `--ink-4` (2.55:1) entirely — that is the one sentence in the product that is not allowed to be hard to read. **The verification badge says what actually happened.** It reads "Worked step by step" when the answer contains visible numeric working, "Explained in steps" when it is structured but not numeric, and plain "AI answer" otherwise — it never claims machine verification, because none is performed. **The contents rail reads the renderer, not an assumption**: most sections become `.nb-card` blocks whose title is a `.hd` containing a decorative `.nb-stamp`, while Concepts and Step-by-Step stay bare `h2`s, so the rail collects `.nb-card > .hd, h2` in document order, strips the stamp text and any leading pictograph, and anchors to the card rather than the heading so the whole section lands in view. **Nothing in the product is a placeholder.** The calculator is a hand-written tokeniser and recursive-descent parser (no `eval`), the unit converter runs on stored SI factors, PDFs are inflated with `DecompressionStream` and their text operators read in the browser so the file never leaves the device, flashcards schedule on Leitner intervals, the mind map lays branches left/right around a centre sized to the taller side so labels cannot collide, and XP is earned per action rather than per minute so the app cannot be farmed by leaving it open. Where something genuinely is local-only — the community board, and current affairs coming from training data rather than a live feed — the UI says so in its own panel rather than implying otherwise. Stack stays vanilla HTML + PHP on cPanel: the brief asked for Next.js/Prisma/Postgres, which the `7solve.7by.in` host cannot run and which would discard the working multi-engine AI router, the account-hub auth and the 7Pay credit paywall |
| 2026-08-13 | 7Solve pricing became server-truth: real plan tiers, enforced AI entitlement, and a currency symbol that follows the visitor | The pricing page has always sold **Spark ₹49 / Solve+ ₹99 / Solve Ultra ₹999** (plus yearly), but `account-hub/pricing.php` only knew `monthly`/`yearly` — so `plan_details('spark')` returned null and **every buy button on 7Solve answered "Unknown plan."** The three tiers now exist in the hub's plan table with their own INR and USD prices, alongside the untouched `monthly`/`yearly` that RemoveBG and VocalRemover still buy. Three rules were recorded with them, because each had been an assumption living in the browser. **(1) A tier is one entitlement.** `tool_grant` used to store the purchased key, so a yearly buyer's wallet read `solve_yearly`, which the client's `PLANS` map does not contain — and they were silently shown as free. The wallet now stores the *tier* (`solve_yearly` → `solve`), so monthly and yearly differ only in price and length and every downstream check stays a simple tier comparison. **(2) Spark's "no AI" is a server rule.** Spark buys the practice tools, not the model, but the only thing enforcing that was `aiGate()` in the browser, where a plan name can be edited — the server granted AI to anyone holding credits. `ai` is now a property of the plan, reported by the hub's `me` and enforced in `7solve/api.php` before the model is called; the client's gate is now cosmetic, deciding only whether to show the upgrade card. The **free tier deliberately keeps AI** — the 30 credits/day allowance buying 3 answers *is* the trial, and the funnel depends on it, so "no AI" applies to a paid plan that excludes the model and to nothing else. **(3) Ultra's 20 credits a day are granted by the database, not by trust.** The hub's `bonus` action already had the right shape (INSERT a ledger row first, add credits only if that insert wins, date from `UTC_DATE()` so a wound-forward clock earns nothing) but `credit_bonus_log` was never created by the schema, so the claim threw and *no daily bonus had ever been paid, on any product*. The table now ships, and the amount falls back to the plan's own `daily` so there is no second place to edit. Two visual consequences. **`money()` is no longer hardcoded to ₹** — the hub prices by country, and `syncPlansFromServer` had silently been a no-op for its whole life (it read `s.price`; the hub only ever sent `amount` in minor units), so the moment it started working an overseas visitor would have seen "₹2.5". Both money helpers and the `.cur` span in the plan card now take the symbol the hub quoted, and `toLocaleString` switches off `en-IN` grouping for non-INR. Rupees remain the default and the fallback, so the Indian student — nearly everyone — sees exactly what they saw before. **The sync salt generates itself**: `sync_salt` fell back to the literal string `'7solve'` when unset, which is the documented default in a public repo, so stored filenames were computable by anyone who knew a hub id — the one thing the salt exists to prevent. A blank setting now mints a 256-bit salt on first use and keeps it in `.sync_salt` beside the data, created with `fopen(…, 'xb')` so two simultaneous requests cannot write different secrets; an explicit salt still wins, for moving data between servers |
| 2026-08-14 | 7Solve 14.1–14.2: no question is shown unchecked, and the student owns their subjects | **14.1 — question quality.** `AI.json` validated the SHAPE of a model's reply and nothing else, so every question path handed model output straight to a student. A model writing "$35,000 × 1.25 = $38,500" produced a question the student then got "wrong", and the miss was recorded against them. A `QC` module now gates all of it, on two stated rules: **a validator that rejects correct questions is worse than no validator** (it silently starves the pool), so every check bails out rather than fires when it cannot parse what it is looking at and numeric comparison allows rounding taken from the decimals actually written; and **free checks run on everything, the model-dependent check is batched**. Tier 1 costs nothing — exactly one choice matches the stated answer, that answer appears among the choices, no duplicate values, no blank choices, an explanation exists, all arithmetic recomputed, and the explanation must reach the answer. Tier 2 re-solves the whole batch in ONE call by a model never shown the key, catching what arithmetic cannot see (a "which is a cost driver?" question keyed to "Net profit" passes tier 1 — the sums are fine — and is rejected by tier 2). Cost: one extra generation per session, 10 → 20 credits. Two false positives found while building are now regression cases and drove the design: `(1,200)` vs `1,200` is an unfavourable and a favourable variance, and normalising the brackets away as punctuation called a perfectly ordinary accounting pair a duplicate; and requiring the answer to be the LAST number in the explanation flagged working that closes with "…= 4,000, so option A", so the test was weakened to "appears anywhere" — appearing nowhere is unambiguous, insisting on last was not. Everything **fails open** (checker unreachable, unparseable, or an answer resolving to no choice) because a flaky checker emptying the pool is worse for a student than a second opinion not being taken; but when every question in a batch is defective the student is shown **nothing rather than a wrong question**. A "⚠️ This question looks wrong" control does three things, not one: deletes the mistake, retracts the evidence via `Learn.events()` — the single choke point every derived figure reads through, so accuracy, weak topics and mastery all stop counting it at once while the raw log keeps the record — and remembers it **by fingerprint**, so the same question generated again never counts. **14.2 — subjects belong to the student.** The product used to decide what you study: pick a level and a course, and a fixed list appeared. That is wrong for anyone whose reality does not match the table — a CMA candidate taking one paper, a retake, a school teaching a combination nobody listed. Subjects are now created by the student, with an empty state that **asks** ("What are you studying?") rather than fabricating cards, and the syllabus list still offered but only as an explicit import. Chapters attach to a subject's **ID, never its name**, so renaming cannot orphan the work underneath — the property the whole model turns on. Topics are plain strings on the chapter, deliberately not their own records: a topic has no state of its own, and adding one should not create a row the student then has to manage. The composer's subject picker puts **My subjects** first in its own `<optgroup>` with the syllabus list below rather than replacing it, so someone with two of their own subjects does not lose access to the rest of their course; names are student-typed now and therefore escaped, where the fixed table had been interpolated raw. A subject card stopped being a `<button>` — it is a link plus its own rename/delete controls, and nesting buttons is invalid and swallowed the taps |
| 2026-08-16 | 7Solve stopped asking the student anything: the level/course/subject picker and the build-your-own-subjects screen are both gone, and the study profile is classified out of the question | Explicit user request with two screenshots — "remove both options completely, keep only Ask your doubt, and say which course or which subject class or exam automatically". This supersedes the 14.2 entry above, whose whole premise was that the student should type their own subjects: right in principle, wrong in practice, because it put an inventory task in front of someone who arrived with one doubt. **Home is now one section.** `#levelPage` — the seven level cards plus the Class/Course and Subject dropdowns — is deleted, and `#v-home` runs Ask → Answer → Notebook with nothing before it. The two `<select>`s survive inside a clipped `#autoProfile` because roughly thirty call sites already read `.value` off them (the prompt builder, `categorize()`, `planRoute()`, the notebook writer, practice, flashcards, the PDF export); detection writes into them, so nothing downstream changed and nothing could silently break. They are clipped rather than `display:none` so assistive tech is not offered a control nobody operates. The searchable index picker is no longer built at boot for the same reason — a button beside an invisible select is a control you cannot see — but `openIndex()` is untouched and is exactly what the single "change" link on the detected line calls; a detection you cannot correct is a trap. **`Detect` rests on three rules.** (1) *A detection is a claim, and a weak claim is shown as weak* — every rule carries a weight, and below the confidence floor the subject chip goes dashed AND the system prompt is rewritten to say the profile is an inference the model should abandon the moment the question does not fit. Handing a model "Subject: Chemistry" with no caveat is how a physics question gets answered in the wrong vocabulary. (2) *It remembers* — "solve x² − 5x + 6 = 0" carries no level, so the last one stands; but a course change **drops the remembered subject**, a bug found by driving the real page, where a Class 9 biology doubt followed by a UPSC question was still reporting Biology. (3) *It only ever writes real options* — course and subject are matched against the existing LEVELS / COURSE_SUBJECTS tables, and an unmatched detection is appended as a real `<option>` rather than assigned as a dangling string. Keywords alone were not enough: the commonest maths doubt in the product contains no maths **word**, because "solve x² − 5x + 6 = 0" is pasted in as-is — so the equation SHAPE is tested, requiring a variable and a digit and an operator, all three, which is what keeps it off "GDP = C + I + G" and off "Answer = B". Difficulty is judged separately from `assessComplexity()`, which was left alone because it also gates which model answers and therefore what the student is charged. **The study space fills itself**: `Subjects.ensure()` files the subject after an answer lands, refusing the generic buckets ("General", "Other", "Current Affairs"), refusing anything the classifier was unsure about, refusing anything merely defaulted into the select rather than read from the question, and capping at 40 so a runaway classifier cannot flood the shelf. The empty state explains the mechanism instead of demanding input. **The verification badge can finally mean something.** It used to describe the SHAPE of the reply ("Worked step by step") because nothing had ever actually been checked. `Verify` now recomputes every arithmetic line the answer states, substitutes claimed roots back into the question's own equation, cross-checks the Answer against the Summary and against the working, checks the result's unit against a unit the question named, and rejects a probability outside 0…1 — all deterministic, in the browser, free, before the student copies a number into an exam. It inherits the QC module's rules from 14.1: **a check that cannot parse its input bails out rather than fires** (brackets, powers, percentages and anything with a variable in it are skipped, because a validator that flags correct work teaches students to ignore it, and an ignored warning is worse than no warning), and **tolerance comes from the decimals actually written** — "1/3 = 0.33" is correct to the two decimals it was written to, and failing it would be pedantry, not verification. A new `.verif.disputed` state — the error tint, the only alarm in the product — fires when an answer disagrees with its own working, and a receipt row underneath lists every check that ran, passed or failed. When nothing could be checked the badge falls back to the old honest wording and the receipt is not rendered at all: there is no state in which 7Solve says "verified" because an answer merely looked tidy |
| 2026-08-16 | 7Solve Phase 2: the chemistry balancer, a code sandbox that really runs, a hint ladder, and the benchmark suite shipped inside the product | Follows the classifier/verification row above, continuing the same rule — **check what can be checked, and never claim what did not happen**. **Chemistry.** "Balance the equation" is one of the most-set questions in the product and one of the few whose answer is decidable without a model, so `Verify.chemistry()` counts atoms on both sides. It is written to refuse rather than guess: state symbols are stripped *before* brackets are parsed (reading `(s)` and `(aq)` as groups would invent a sulphur atom in every precipitate), ionic charges bail the whole equation out, and an element appearing on one side only is treated as a fragment rather than an imbalance — that last rule is what stops `x -> 0` and `Input -> Process -> Output` becoming failed checks. The first build had the left-hand side captured by a lazy regex that excluded `+`, so `2H2 + O2 → 2H2O` was read as `O2 → 2H2O` and every balanced equation in the test set failed; sides are now captured generously and eaten inwards one word at a time until what remains parses, which also buys the ability to read an equation out of a sentence ("The balanced equation is 2H2 + O2 → 2H2O, so…"). **The sandbox is real.** `codeTool` previously sent code to a model and displayed the reply, including for "What is the output?" — a prediction sold as an output, which is precisely the claim §11 forbids. JavaScript now executes in a Web Worker built from a Blob: its own thread so an endless loop cannot freeze the page, no DOM because a Worker has none, `fetch`/`XMLHttpRequest`/`WebSocket`/`EventSource`/`importScripts` deleted before user code runs, a hard timeout that terminates the thread, and console output capped at 200 lines. A syntax error never reaches the worker's own try/catch — the script fails to compile — so `onerror` is wired as the second path. "What is the output?" now **runs the code first** and hands the model the observed result with an instruction not to contradict it. The other seven languages cannot run in a browser and the UI says so in those words rather than hiding the button, because a student pasting Python should learn why, not wonder where it went; executed output is the only place a monospace console appears, so a real result never looks like a predicted one. **Hints became a ladder** (§21): five rungs from "a small clue" to "nearly there", each naming both what it may say *and* what it must still withhold, because a model told only "give a bigger hint" jumps to the solution on the second press. Rungs accumulate on screen rather than replacing each other — seeing 1–3 together is how the shape of the approach is learned — and the filled/empty rung markers exist so "how much is left before the answer" is readable at a glance, which is what makes stopping early feel like a choice. The ladder resets the moment the question is edited, not on the next press, since rung 4 of the previous question is a nonsense answer to a new one. **The benchmark suite ships in the build** (§46/§47) rather than living in a test folder on one machine, so "is the copy students are actually using still correct?" is answerable on the live site in under half a second: 34 cases across the classifier, arithmetic, root substitution, answer consistency, chemical balancing, unit rules and the sandbox, run from Settings, each with a verified expected answer. Several are regression cases for bugs that really occurred while building this — the lazy chemical left-hand side, the remembered subject that survived a course change, and the arithmetic scanner learning the difference between algebra and a sum. Async and sync cases share one list and a case that throws is recorded as a failure rather than ending the run. The footer states the limit plainly: these cover only what can be checked without an AI, and say nothing about whether a model's reasoning is sound |
| 2026-08-16 | 7Solve Phase 3–5: exam mode, live sources with authority tiers, a feedback loop, a quality dashboard, real Python, and the credit races closed | Completes the upgrade spec's remaining phases. **Credits were not safe, and the bug was ordinary rather than adversarial.** Every counter did read-then-write with `LOCK_EX` on the *write* only — the read sat outside the lock, so two requests landing together both read 40 and both wrote 50, and a student got two answers for one answer's worth of credit. A double-tap on a phone is enough. `bill_atomic()` now holds one exclusive lock across read, decision and write, opened `c+` (not `w+`, which would truncate the counter *before* taking the lock). Separately, `?action=charge` had no request identity at all, so a charge POST retried after a lost reply on mobile data spent a second time for an answer shown once — §54's exact case. The browser now mints one id per delivered answer, scoped server-side to the caller's own token so one student cannot replay another's id; a known id replays its stored result, a concurrent duplicate is told the charge is already in flight, and a **refusal is deliberately not stored** so "you had no credits a moment ago" cannot be replayed to someone who has since topped up. **Exam mode is not practice with a clock**, and that is the whole design: practice marks each answer and moves on, an exam marks nothing until you submit, lets you move anywhere in the paper, and makes guessing cost something. So it does not reuse `Runner`, whose entire shape is "answer, be told, move on" — the one thing an exam may not do. It reuses the question generator and the Mistake Book, which is where the value is. Negative marking applies only to a wrong *attempt*; a blank scores zero and never negative, which is how every Indian competitive paper works and is why "should I guess?" is a decision worth teaching — the report says so explicitly ("leaving them blank would have scored 8"). The deadline is an absolute timestamp rather than a ticking counter, because a backgrounded phone stops firing intervals and a paper that pauses while you take a call is not an exam; everything autosaves on every interaction, so a closed tab resumes with the *real* time left rather than a fresh clock (an exploit) or a lost paper (worse). Answers are held back completely until submission — verified by test, not assertion. On a phone the palette moves **above** the question: you need to see where you are before you answer, and a sticky sidebar has nothing to stick to when the column is the whole screen. **Live sources (§13/§14).** A model's weights have a date on them, and answering "who is the current CJI" or "when is the CAT exam" from memory is the most damaging thing this product could do, because the student cannot tell that answer from a correct one. Time-sensitive questions now fetch real pages first, scored into authority tiers — government and official bodies above universities and research, above news, above encyclopaedias, above everything else — and the tier is *shown* rather than used to silently pick a winner, so a student can see when the only thing found was a blog. There is no branch that invents a citation: when nothing is reachable the prompt is rewritten to answer from training data **and say so**. The endpoint fetches URLs, which makes it an SSRF risk on shared hosting where the metadata service and the neighbours are one hop away, so `url_safe()` allows only public https, resolves the host and rejects private, loopback and link-local ranges, refuses userinfo (`https://evil.com@127.0.0.1/`), and follows at most one redirect *re-validated* — `CURLOPT_FOLLOWLOCATION` would have walked straight past the check. Suffix spoofing (`gov.in.evil.com`) gets no authority. All 28 SSRF cases are tests. **The feedback loop (§45) treats a student as an opinion, not an oracle**: a rating sends no content at all, only a report sends the question and answer (and the button says so), and the value is in the *disagreements* — the dashboard's top three lists are reported-as-wrong, "our checker said no but the student said yes" (a false positive worth fixing) and "our checker said yes but the student said no" (a method or level problem no calculator can catch). **The quality dashboard is off by default** and refuses to render until `admin_token` is set to at least 16 characters, compared with `hash_equals`: a dashboard that is public by default would leak what students are asking, so "not configured" fails closed rather than open. Two logs, deliberately separate — `ops.jsonl` carries metadata only (never a question, never a token, never an IP) because "is the service healthy and what does it cost" does not need to know what anyone asked; `quality.jsonl` carries content only where a student chose to send it. Both roll at 8MB so a busy month cannot fill the account's disk quota and take the site down. **Python really runs.** Pyodide (CPython on WebAssembly) executes in a worker with one honest exception stated in the UI: `importScripts` and `fetch` must survive long enough for the engine to pull its own wasm, so the network is open during *engine load* and both are deleted before a line of student code runs — verified from inside Python (`js.fetch is None`). The ~10MB download is stated in the interface and happens only on an explicit press, because on Indian mobile data that is the student's decision to make. A hung Python cannot be interrupted from outside, so the timeout throws the whole engine away and the next run pays the load again — the honest cost of being able to stop an endless loop. C, C++ and Java still cannot run in a browser and the UI says exactly that rather than hiding the button |
| 2026-08-16 | 7Solve accuracy fix: a real algebra engine, mandatory verification before display, and the rule that a heuristic never outranks a proof | Reported against `x² + y² + 1 = 3xy`, where an answer claimed (5,3) was a solution (35 vs 45), that `(x,y) → (3y−x, x)` preserved the equation, and that (2,1),(5,3),(13,8),(233,144) were the solution sequence. All four claims were false and **nothing in the product noticed**, because the checker's equation reader was a regex that rejected any letter but `x`. A two-variable question produced *zero* checks and went straight to a student. That is not a bug you patch per-question, so the checker was rebuilt around something that can actually evaluate mathematics. **A tokeniser and recursive-descent parser replace the coefficient hack.** It handles multivariate expressions, implicit multiplication (`3xy` → 3·x·y, and the reason a single-letter variable rule is right for school algebra), `^` and the ² ³ ⁴ superscripts, unary and unicode minus, and sqrt/abs/exp/log — parsing to a tree that can be evaluated under a binding. That one capability is what makes every new check possible: substitute a claimed solution, iterate a claimed transformation, compare two sides of a claimed identity at random points. **The equation reader has to tell algebra from prose**, because the same rule that correctly reads `3xy` as a product reads `Solve` as s·o·l·v·e — which it did, yielding thirteen "variables". Two filters fix it and the balance between them was found by testing, not taste: a run of **five or more** letters is a word (an earlier three-letter threshold was stricter and wrongly rejected `3abc` in the Markov equation, killing real checks), and the distinct-variable count is capped at three, which is what actually throws out short words like "in" and "let" that would otherwise smuggle variables aboard. **Transformations are tested, not trusted** (§8). The reported map is instructive: `(3y−x, x)` genuinely works once from (2,1), which is how it survived review — so the check applies the map to *every* independently-found solution and **iterates it three times**, because one hop can pass by luck. Known solutions come from a bounded integer search, never from the model, so the claim is checked against something the model did not produce. The counterexample shown is the one a student can follow: positive solutions are tested first, so the report reads "from (1,2) it gives (5,1), where 27 ≠ 15" — exactly the derivation in the bug report — rather than an equally fatal but unpersuasive negative pair. **Verification is now a gate, not a badge** (§5/§10). A failed proof means the answer is *not displayed*: it is sent back to the model with the exact failing checks and a demand to substitute before claiming, and re-solved **once** — once, because a model that cannot fix it given the counterexample will not fix it in three passes, and each pass costs the student time. Both attempts share one idempotency id, so being re-asked because *our* checker rejected the first answer is never billed twice. If the second attempt also fails, the better one is shown with the failure stated loudly; hiding it would leave a paying student with nothing. **The four states §9 permits are the only ones that exist**, and "✓ Verified" now requires a real substitution, recomputation or atom count — never a well-written argument. Which forced the rule that matters most: **a heuristic must never outrank a proof.** Two kinds of check live here — proofs decide (a substitution holds or it does not), hints smell ("the final value never appears in the working"). The hint marked a completely correct answer as failed the moment the answer was a *set of tuples* with no single headline number. Only a failed proof can now produce "✗ Verification failed"; a failed hint downgrades to "⚠ Partially verified" and stays visible in the receipt for the student to weigh. A checker that cries wolf gets ignored, and an ignored checker is worse than none. **Two further bugs were found by testing generality rather than the reported question** — required, since §12 forbids fixing only this problem. Three-variable equations were rejected outright by the word filter (Markov `a²+b²+c²=3abc`), and the identity checker gave every variable the *same* value at each test point, so the false factorisation `a³−b³ = (a−b)(a²−ab+b²)` evaluated 0 = 0 and passed. Variables now vary independently. The suite grew from 35 to 57 cases, 22 of them on the maths engine, covering Pell equations and their transformations, Markov triples, cubics and quartics, true and false factorisations, and the refusal to judge trigonometry whose degree/radian convention is not pinned down |
| 2026-08-16 | 7Solve logic layer: the conclusion is now checked against the reasoning, not just the arithmetic | Three more failures reported, all of a kind the substitution engine structurally could not see, because in each of them **the arithmetic was right and the answer was still wrong**. (A) "n = 1 is a solution", followed by the answer's own line "13 does not divide 32". (B) the invalid Vieta map, already covered. (C) the worst: a *correct* factorisation of n⁵+2n⁴+3n³+3n²+2n+1 as (n²+n+1)(n³+n²+n+1) — which proves the divisibility for **every** integer n — concluded with "n = 3". Every step true, conclusion false. Substitution cannot catch that, because n = 3 *is* a value where the property holds; it simply is not the only one. So the new question the checker asks is not "is this true?" but **"is this all?"**, and that is decidable the same deterministic way: take the condition the answer itself states, enumerate the domain, and see what else satisfies it. Four engines were added. **Divisibility** (§6) reads "A divides B", "A ∤ B", "B is divisible by A" and tests them — a numeric claim is one modulo, a claim in a variable is swept over 81 integers, and a "does not divide" is the same machinery with the expected answer flipped. **Completeness** (§14) is the one that catches (C): when an answer presents a single value as the answer to a "find all" question, every other value in the domain is tried against the condition, and n = 1, 2, 4, 5 come back as counterexamples. **Condition checking** (§7) catches (A) by treating the QUESTION as the authority rather than the answer's internal consistency: whatever value the answer claims is substituted into the divisibility the question actually states. **Quantifier collapse** (§11) flags "holds for every n … therefore n = 3" — but as a *hint*, never a proof, because it reads prose. Extraction turned out to be the hard part and produced the sharpest rule: the tokeniser reads every letter as a variable, which is right for `3xy` and catastrophic for `So n²+n+1`, read as S·o·n²+n+1 — three variables, so the check silently declined to run on the very claim it exists to test. Operands are now taken by **walking outward from the keyword and stopping at the first word**, with a stoplist that treats two-letter words like "so", "is" and "of" as words even though `xy` stays legal algebra. **Four false positives were found by testing correct answers rather than broken ones, and each is now a regression test.** "n = 3" was being reported as a failed *identity* — it is an assignment, and a side that is a lone variable is now skipped, which had been failing essentially every algebra answer in the product. `2n` in a number-theory question was read as 2 **newtons**, putting "your answer has no unit" on pure mathematics; single-letter units now require the question to mention a physical quantity. "2n = 6, so n = 3" inside correct working was read as the root n = 6, which then failed substitution and turned a right answer red — the root pattern now refuses a variable preceded by an alphanumeric. And the completeness engine stopped at its sixth counterexample and then **discarded all six for insufficient coverage**, switching itself off on exactly the case that motivated it: coverage now gates only the *negative* conclusion, since finding one counterexample needs one value however early the loop stopped. Counterexamples are swept positives-first for the same reason the transformation check is: "it also holds for n = 1, 2, 4" teaches something, "for n = −30" reads as a technicality. Known limitation recorded honestly: the parser cannot follow pronouns, so "So n²+n+1 divides **it**" yields no verdict rather than a wrong one — fail-open by design, and the reason the receipt distinguishes "⚠ Unable to verify" from "✓ Verified" |
| 2026-08-17 | 7Solve answer page rebuilt: fourteen mandated sections and the Contents rail replaced by five, exact-form mathematics made verifiable, and the model map un-rotted | Explicit user request with a screenshot: the answer page was cluttered, and the left "Contents" rail listing Final Answer / Definition / Concepts & Formulas / Step-by-Step / Alternative Method / Important Notes / Common Mistake / Exam Tip / Memory Trick / Real-Life Example / Teacher's Remark / Practice Question / Summary / Revision Box had to go. **The rail was a symptom, not a feature** — an answer that needs a table of contents is an answer that is too long — so the fix was upstream in the prompt, not in the CSS. The default format is now Final Answer → Method → Step-by-Step → Verification → Final Result, with Common Mistake / Alternative Method / Note available only when *this* question needs them and capped at two; the prompt says in as many words that an answer padded with generic sections is a worse answer, and that length follows the question rather than a target. `buildTOC()` survives as a no-op so its one caller did not need finding, and `#answerBody .nb-toc{display:none}` survives so a *saved* answer written in the old format still renders instead of suddenly growing a rail. Layout became one centred column capped at 980px; the answer value is the largest type on the page (31px desktop / 25px phone) in a card whose left edge carries the verification state. **The "Checked ✓" stamp was a lie and is gone.** It was printed on the Final Answer card unconditionally, by the renderer, whether anything had been checked or not — the exact false confidence the verification work exists to remove. The stamp is now written by `paintVerif()` from the real result, in words as well as colour, and is left blank when nothing could be checked. **Exact form is now first-class, and that took real work.** The reported answer `x = 3/2` for `√(x+6) + √(2x−1) = 5` was wrong, and so was the value in the brief: `88 − 30√7` gives 7.856, not 5 — it is `(82 − 30√7) + 6`, the value of x+6. The real root is **82 − 30√7**, and the reason nothing caught any of it is that the root reader only ever matched a leading integer, so an exact answer could not be checked at all. The reader now parses the whole right-hand side with the algebra engine (which learned bracket-free roots, `30√7` → 30·sqrt(7), so `√25200 = 60√7` evaluates), expands `a ± b` into two candidates rather than degrading to `a`, and reads only the **Final Answer and Final Result sections including their heading lines** — models write the value straight into `## ✅ x = 82 − 30√7`, and reading section bodies alone found no answer and silently checked nothing. A `deLatex()` pass converts `$\sqrt{x+6}$`, `\frac{a}{b}`, `^{2}` and the symbol macros to the plain-text maths the app already renders, run once in `solve()` so the renderer, verifier, notebook, PDF and read-aloud all see the same thing; code fences are guarded. Converting beat loading KaTeX: it is no new dependency on a cheap phone, and it also hands the verifier something it can evaluate. **Five false positives were found by testing correct answers, and each is now a regression test.** Equation-solving steps were each reported as "not an identity" — a conditional equation holds only at its roots, so it disagrees at every random point; identity checking now runs only on the unambiguous shape, a product of brackets. A numeric tail was scraped out of algebra (`x² − 64x − 100x + 1024 − 600 = 0` read as `1024 − 600 = 0`), so an arithmetic check now scans back to the nearest delimiter and refuses if a variable appears. A candidate the answer itself calls extraneous is no longer treated as a claim — scoped to its own **line**, because a character window reached onto the next line and skipped the real answer too, leaving the answer with no verification at all, which is worse than the false positive it was meant to fix. And `:not()` inherits the specificity of its argument, so an exclusion written to protect the 31px answer value outranked it and shrank it to body size; the value is now selected with two classes and a child combinator instead. **The model map had rotted and the live site was walking dead engines.** Probing every provider found `gemini-2.5-flash`/`2.0`/`1.5` returning 404 "no longer available to new users", `cerebras llama-3.3-70b` 404 (that account serves glm / gpt-oss / gemma), every OpenRouter `:free` model 404 "unavailable for free", and GitHub Models 410 mid-retirement. A stale name here does not fail loudly — the router collects 404s and the student is told "all engines failed" — so the map now lists only what returned real text when checked, with the findings and the date recorded beside it so the next person knows to re-probe rather than guess **Follow-up: the rail now steps aside for an answer.** The shell is sidebar | work | rail on every view, which left the solution about 740px at 1440px — fine for prose, cramped for a derivation. Once an answer is on the page the rail is dropped and the grid closes up, giving 1074px at 1440px and 914px at 1280px, both inside the 900–1100 the brief asks for. Done by CLASS rather than media query because it is about state, not screen size: the rail is useful on the composer and on every other view, and it returns the moment the answer is cleared, a new doubt is started, or another view is opened. Scoped to >1240px, which testing forced — .app.wide-answer is two classes and outranked the single-class rules inside the responsive breakpoints, so unscoped it re-imposed a 252px sidebar track on a 375px phone and squeezed the answer into 222px. **Live testing across 15 question types found four more faults, three in the checker and one in the router.** A factored equation being solved — "(x − 3)(x − 4) = 0" — was reported as a failed identity, so a correct quadratic came back ✗; an identity relates two EXPRESSIONS, so both sides must now contain a variable, which separates factorising from solving. "4/36 = 1/9" was truncated to "4/36 = 1", failing a correct probability answer; the right-hand side may now be a fraction and is EVALUATED, which verifies the simplification instead of skipping it — and an exact fraction is compared exactly, because the decimals-written tolerance rule gave it 0.5 and passed 4/36 = 1/8. A chemistry answer was judged on the unbalanced equation from its own working rather than the balanced one it gave, so chemical equations are now read from the answer zone with the full reply only as a fallback. And the router **aborted the entire engine chain on the first HTTP 429**: a rate limit from Gemini says Gemini is spent, and nothing about Groq, Mistral or Cerebras, which are separate accounts — seen live with Gemini free tier at its 20-request cap taking the product down while three working providers sat unused. A 429 now marks that provider spent and moves on, and is only shown to the student when every provider is rate-limited. **Reported live: a correct optimisation answer came back "✗ Verification failed", and the five failing lines were all mine.** `2√3/3 + 1/3 = 13/3` was being read as `3/3 + 1/3 = 13/3` — the arithmetic scanner started matching at the digit AFTER a radical, so it recomputed the tail of a surd and found it did not add up. All five reported failures reproduced character-for-character and are now regression tests. The scanner refuses a match preceded by a radical, and also walks back over the digits and operators of the expression to catch a match that starts a few characters inside one. That is the worst false positive the checker can produce: it tells a student their right answer is wrong. **The prompt now separates solving from writing.** The same answer narrated its own trial and error — "Wait, let me re-evaluate", "Let me double check", the same quantity recomputed three times — which reads as an answer that cannot be trusted even when the final value is right. Rule 6 was rewritten from "verify before you answer" to "solve first, then write": work it out and check it BEFORE writing, present one clean derivation, never narrate the process, say each thing once, and let length come from rigour rather than repetition. It also bans the unjustified hand-wave — "the maximum occurs when two variables are equal" is not a reason, it is the thing that needs one. **And feasible is not optimal.** For a maximum or minimum, showing the claimed point satisfies the constraints establishes only that it is a candidate; whether anything else does better is the actual question. No extremum proof is attempted — that needs symbolic optimisation this engine does not have — so an optimisation question can no longer reach "✓ Verified" on a substitution alone. It falls to "⚠ Partially verified" with the limit stated in the receipt. The check sits in the hint tier on purpose: it is a statement about what our checking did NOT establish, not a claim that the answer is wrong. **Global optimality is now computed, not asserted.** The reported answers kept resting on "the maximum occurs where two variables coincide" — an assertion, and the thing that actually needed proving. For three variables under the two elementary symmetric constraints it need not be asserted at all: fix x, and y+z = s−x with yz = q−x(s−x), so y and z are roots of ONE quadratic and are real exactly when its discriminant is non-negative. The feasible set is therefore one-dimensional and can be swept exhaustively; the double-root point falls out as a RESULT. The check reports the true extremum, where it occurs, and whether the answer matches. Three bugs were found building it, each by testing against ground truth computed separately rather than trusting the engine: an earlier version solved the cubic in t and bisected for three roots, and the outer brackets did not always contain a sign change, so bisection returned an interval endpoint and the "roots" did not even sum to s; `grabExpr` refused the objective `xyz` because three letters with no digits read as a word; and an exact claimed value like `(594 + 22√3)/9` was being read as 594, which made one wrong answer look like a different wrong answer. **The receipt also contradicted itself.** With eleven checks and the list capped at ten, the single failure fell off the end — so the panel was headed "what did not add up" above ten ticks. Failures are now listed first: what went wrong must never be the thing that gets truncated. **A wrong step is not a wrong answer, and the badge now says which.** An olympiad answer came back with the right result — max 4, at the three CYCLIC points, having resisted both the symmetric-point trap and the two-equal shortcut — and one wrong line in the middle: an AM-GM factorisation out by a factor of two, which the identity checker caught correctly. Reporting that as "Verification failed" tells a student their correct answer is wrong, which is the same disservice as missing the error. Proof-tier checks are now split by what they bear on. An ANSWER check tests the claimed result against the original question — substitute it, sweep for the true optimum, count the atoms — and only those can condemn the answer. A WORKING check tests a line of the derivation, and a failure there gives a new state, `stepfail`: "a step does not hold", amber rather than red, with the receipt headed "the answer stands, but a step does not". This is what the earlier brief asked for when it wanted a right answer with an invalid step detected without marking the whole response failed. |
| 2026-08-18 | 7Solve routing reordered from the ops log, and the dashboard's Models panel stopped counting failures as answers | The quality dashboard's first real day of production data paid for itself immediately. Of 65 AI requests only 24.6% returned an answer, and p95 latency was 59005ms against a `total_budget` of 60s — the requests were not slow, they were running out the clock. The mechanism is the per-attempt timeout in `api.php`: `CURLOPT_TIMEOUT => max(8, min($perCall, $left))`. Providers that fail still consume the budget, so once the chain had spent ~46s collecting a 429 from Gemini and a 410 from GitHub Models, whichever provider would actually have answered was handed the 14s stub that remained, and timed out. The eleven "Operation timed out after 14000 milliseconds" rows were a symptom, not a cause. Fixed by ordering the reasoning chain on evidence rather than on preference: Cerebras leads because it is what replies; GitHub Models is unwired entirely, because a scheduled upstream retirement is not a key that rotation can fix; and the deep chain gained Mistral in place of a line reading `MODEL_MAP.github.deep` — a key that has never existed, so deep questions had been getting two real attempts while the comment beside them claimed three. The dashboard was erring in the same direction: its Models panel was headed "which engine actually answered" but incremented on every row, so `openai/gpt-4o` showed 21 requests while answering none of them. It now reports answered and tried as separate columns, because a panel used to choose a model must not present its worst performer as its third-best. |
| 2026-08-18 | 7Solve verification engine ported to PHP for the public API, with a parity harness as the price of admission | The spec asked for a REST `/v1/solve`, which the existing architecture cannot serve: the browser orchestrates the whole pipeline — detect, route, verify — and `api.php` is only a key-holding pass-through, so a server-side endpoint means the engine must exist on the server. Offered the choice between a Node service running the existing JavaScript unchanged and a second copy written in PHP; the second was chosen to keep everything on cPanel. That decision is defensible but it buys a specific, nasty failure mode — not a crash, but the API reporting `verified` on an answer the website reports `disputed`, where a customer has no way to tell which copy is right. So the port does not ship alone. `parity.js` extracts the Algebra module straight out of `index.html` (never a copy, so it always tests what actually ships), drives the same 459-case corpus through both engines, and exits non-zero on any disagreement. It was then tested by deliberate sabotage rather than assumed to work: changing `cbrt` to PHP's `pow` semantics was caught, but the first version sailed past a tolerance loosened from 1e-9 to 1e-3 because no case in the corpus landed between the two thresholds — so near-miss bindings were added specifically to pin the constant, and the sabotage was repeated until both classes of drift failed loudly. The corpus keeps every historical bug permanently: implicit multiplication in `3xy`, the surd tails like `82−30√7` that once failed to parse, `cbrt(-8)` where JS and PHP genuinely disagree, and the strings the tokeniser must refuse outright. A verifier that can drift silently is worse than no verifier, so the harness is the deliverable, not an accessory to it. |
| 2026-08-18 | 7Solve cache keyed on the question instead of the engine, after the dashboard showed a 3% hit rate | The key was sha256(provider | model | whole payload), which meant it could almost never hit. The router changes provider on every call by design, so the same question asked twice was two entries; the model was in there for the same reason; and the entire payload including the system prompt was in there too, so every prompt edit silently emptied the cache — changing the routing order earlier the same day wiped it completely. What actually decides whether two students deserve the same answer is the QUESTION and how the answer should be shaped: the level it is pitched at, the language it is written in, and the subject it is treated as. Not which engine happened to be up. The browser now sends those three as cacheTag and the server keys on that plus the normalised conversation text; when no tag arrives — an old cached page, or a direct API caller — it falls back to hashing the system prompt, which is exactly the old conservative behaviour, a poor hit rate but never a wrong answer. Normalisation is deliberately limited to case and whitespace: in this product x^2-4 and x^2+4 are different questions, and anything cleverer risks serving one as the other. Measured on simulated traffic of ten requests across three providers with a prompt edit partway through, the old scheme hit 0 of 10 and stored 10 entries; the new one hit 6 of 10 and stored 4. Nine safety properties were asserted before shipping, the ones that matter being that a different level, language or subject never shares an entry, and that a follow-up turn is never served the answer to the question that preceded it. Billing was left where it was, ahead of the lookup, so a cached answer still costs the student credits and only costs us no quota. |
| 2026-08-18 | 7Solve gains a deterministic solver at /v1/math/solve, and deLatex stops ignoring braced superscripts | The spec asks that calculations prefer deterministic computation over an LLM, and the economics agree: a solver that needs no provider has the same zero marginal cost as /v1/verify, which is the only reason either is worth selling. Solver::solve takes the exact path where it can prove one applies and refuses everywhere else, because a solver that guesses is indistinguishable from one that knows. Linear and quadratic are identified by fitting a, b, c from three points and then CHECKING the fit at three more — fitting alone proves nothing, since any three points lie on some parabola, so the verification step is the argument rather than a formality. Anything else falls to bisection on sign changes and is returned with complete:false and a note naming exactly what that misses: roots outside the scanned range, and repeated roots that never cross zero. Every root, exact or numeric, is substituted back before it is returned; the solver does not mark its own homework any more than a model does. The exact surd form was initially shipped as (164 + √25200)/2, which is arithmetically correct and useless — no student writes that when the answer is 82 + 30√7 — so the largest square factor is now pulled out and the fraction reduced by the gcd of all three parts, and each form is verified by parsing it back through the algebra engine and comparing to the numeric root. Building it also exposed a bug in deLatex, in both engines: its fast path returned early unless the text contained a backslash or a dollar, so "x^{2} - 164x + 424 = 0" — no delimiters, exactly how a model writes it inline in markdown — was never converted, the tokeniser cannot read ^{2}, and a perfectly ordinary equation looked unparseable while producing zero checks. |
| 2026-08-18 | A correct answer was being marked wrong on production, and the parity harness passed clean throughout | Testing the deployed build rather than trusting the deploy found it. "x^{2}+y^{2}+1=3xy" with the correct answer (1,1) came back DISPUTED, and the receipt showed why: the extracted equation was "+1=3xy". EQ_CHARS contains no braces, so findEquation could not span the {2} and settled for the longest run it could read, then substituted a perfectly correct (1,1) into that fragment and got 1 ≠ 3. Telling a student their right answer is wrong is the single worst thing this product can do, and it was live. The cause was drift of exactly the kind the two-engine port was supposed to be guarded against: deLatex was called on the question inside Checks::run on the PHP side, but the browser reached substitution() directly and never stripped LaTeX from the question at all. The harness did not notice because it only ever compared the Algebra layer — parse, evalAt, holdsAt — where the two engines agreed perfectly. A harness that checks the layer underneath the bug passes while the product is broken, which is worse than no harness, because it is trusted. Fixed by deLatexing the question in the browser too, and by adding a second corpus of nineteen full question-and-answer pairs judged end to end. The extension was then validated by reverting the fix and re-running: the algebra corpus reported zero disagreements while the verdict corpus reported two, naming the exact case. Coverage now runs to 478 cases. |
| 2026-08-18 | 7Solve marks a student's answer at /v1/answer/check, deterministically and with a configurable scheme | §9 asked for a check-my-answer endpoint, and it is a different product from /v1/verify rather than a variation on it: that endpoint asks whether an AI got it right, this one asks whether a STUDENT did, and the buyer is a school or coaching centre rather than an AI company. It needed no new engine. Solver::solve already produces the true roots and the substitution reader already extracts claimed ones, so grading is set comparison — which means the mark arrives with a proof attached and costs nothing per call. The root extractor was lifted out of substitution() into Checks::claimedRoots so the grader reads a student the same way the verifier reads a model; marking someone against a different parse of their own writing than the rest of the system uses is how a grader becomes unfair without anyone noticing. The verdict that justifies the endpoint is INCOMPLETE: a student who writes x = 2 for x² − 4 = 0 has done real work and missed a root, and every teacher marks that differently from one who wrote x = 5. The first version hard-coded a full mark deducted per wrong value, which scored zero for a student who found one of two roots and added an error — no teacher would give that, so the penalty became a parameter defaulting to zero, with 1.0 available for genuine negative marking. UNMARKABLE returns marks as null rather than zero, so a complex-root question or an unfamiliar phrasing can never be summed into a total by accident; a student must not lose marks because the grader could not read the question. |
| 2026-08-18 | 7Solve reads units: dimensional analysis closes the physics blind spot in both engines | The verifier understood algebra and arithmetic and nothing else, so a physics question returned unverified almost every time — honest, and useless to a physics customer. This catches the most catchable physics error there is: an answer in the wrong KIND of unit. "Find the acceleration" answered with 25 N is wrong before any arithmetic is examined, and that is provable from dimensions alone without knowing the right number. It deliberately says nothing about magnitude, since 9.8 m/s² and 980 m/s² are dimensionally identical; it is a sieve for one error class, not a physics engine, and claiming more would make it dangerous. The precision work was in deciding when to stay silent. A first version asked only whether a quantity word appeared anywhere, so "calculate the force on the mass" named two table words and was abandoned as ambiguous — but mass is the setup there, not the question. Only quantities in an ASKED position now count, straight after an interrogative or after a conjunction adding a second request, which keeps "find the force and the acceleration" correctly ambiguous while letting the ordinary phrasing through. Twenty cases pass, seven of them requiring silence. A wrong dimension is classed answer-level beside substitution rather than step-level, because it condemns the answer whatever the working did. The check exists in units.php and in index.html, so fourteen physics cases went into the parity corpus at the same time — without them the harness would have been blind to this exactly as it was blind to the LaTeX divergence that reached production earlier the same day: green suite, broken product. Coverage is 492 cases, and the physics half was validated by sabotaging a newton into an acceleration and confirming the harness named the two cases it should. |
| 2026-08-18 | Question integrity: 7Solve now checks that it solved the problem that was actually set | A test exposed the failure every other check in this product is blind to. A question containing 3x+y was solved as 3(x+y); the working was clean, the arithmetic held, substitution passed — and the answer was wrong, because it was correct for a different problem. Everything built so far verifies ANSWER correctness against the question's equation, which is worthless if the equation was misread. The check compares the equation in the question against the one the reply restates, by evaluating both residuals at eight scattered points and allowing a constant factor, so 6x+2y=14 agrees with 3x+y=7: the shape must match, not the writing. Not crying wolf was the whole difficulty. An answer legitimately contains equations that differ from the question — x²=4, then x=±2 — and flagging those would make the check worthless within a day, so it reads only the restatement at the top of a reply, only compares an equation carrying exactly the question's variables, and skips solution statements of the form variable-equals-constant, which had flagged a perfectly correct "x = 2 and x = -2" as a misreading. Twelve cases pass, seven of them requiring silence. Building it also uncovered an older bug reaching much further: findEquation fed trailing sentence punctuation to the parser, so "2(x+3) = 11." tokenised "11." and refused, and any equation ending a sentence was invisible to EVERY check built on that function, substitution included. The prompt was changed at the same time to open each answer with the equation as understood, which the student can check at a glance — and which has the side benefit of putting a faithful restatement first, where the integrity check reads it, instead of a grouped intermediate step. Both engines carry it and the parity corpus grew to 502 cases. |
| 2026-08-18 | Three trust layers reported separately, a solution-to-final trace, and a deLatex bug that manufactured the very error being hunted | Feedback split verification into two questions the product had been conflating: did we understand what was asked, and is the solution correct? Only the second was ever checked, so a flawless answer to a misread question passed everything. The acceptance test — 3^(x+y) solved as 3(x+y), and the reverse, and three formatting variants — already passed against the integrity check built earlier, so the new work was the third layer and the reporting. trace() asks whether the stated answer follows from the working shown: a model can derive x = 2 across four lines and print x = 4, and nothing else here would notice. The rule is deliberately weak, because a strong one would cry wolf — a final value must merely APPEAR somewhere in the working — and it stays silent when the working states no value at all, since flagging "divide both sides by 3" would be exactly the false positive the spec warns against. Its first version was broken in a way worth recording: it subtracted the claim zone by string replacement, but claimZone BUILDS its value from two sections and is not a substring, so the replace did nothing, the "working" became the entire reply, every value trivially appeared in its own working, and the check could never fail. Reading the Steps section by heading fixed it. The three layers are now reported as separate statuses rather than averaged, because "arithmetic verified" is true and useless when the question was misread — the sum really does add up, for the wrong problem — so overall is a conjunction and never a majority. Building the parity cases then caught a genuine correctness bug of exactly the kind being hunted: deLatex stripped the braces from a compound exponent, turning 3^{x+y} into 3^x+y, which is (3^x)+y. A pure formatting difference had been silently manufactured into a mathematical one by the very function meant to normalise it. Fixed in both engines; braces now become parentheses. Coverage is 512 cases. |
| 2026-08-18 | Almost every correct answer was showing "⚠ Partially verified", and the cause was ordering, not mathematics | Reported from real use, and three separate faults were behind it. The visible one predated the day: the state machine tested for a hint-tier miss BEFORE it tested for passing proofs, so a single hint outranked every proof that had succeeded. The hint tier exists precisely because those checks interpret prose and must never fail a correct answer on their own — and one of them, the consistency check, was reading the 4 in a working line "x² = 4" as a rival claim against an answer of 2. Four passing proofs, one prose misreading, and the badge said partially verified. Proofs now decide the badge and hints are listed in the receipt without a veto. The second was self-inflicted the same day: the new "Understood as" block echoes the QUESTION back, and every check except integrity was reading it as part of the answer, so the question's own numbers became competing claims. Only integrity sees that block now. The third was in the API: FULLY_VERIFIED demanded that all three trust layers had RUN, but NOT_CHECKED is not a failure — a two-variable question, where the trace layer cannot apply, could never be better than PARTIALLY_VERIFIED. It now requires that nothing contradicted the answer and that the substantive layer actually ran; a trace alone is still not enough to claim it, since "the final value appears in the working" is true of plenty of wrong answers. A label that reads "partially verified" on nearly everything is worse than no label, because it trains a student to ignore it — which is the same failure as a badge that says verified without checking, arriving from the opposite direction. |
| 2026-08-18 | The chapter reader was displaying binary as text, and the integrity check was flagging an algebraic identity | Two reports, both false confidence. The reader decoded EVERY unfiltered PDF stream as latin1 and kept whatever came back, guarded only by whether the bytes happened to contain "Tj" — which binary does by chance. Embedded fonts, colour profiles and image data all reached the page, and a student who uploaded a chapter got a wall of mojibake. Showing nothing would have been bad; showing confident nonsense is worse, because it looks like the file was understood. Streams declaring a font, image, ICC profile or metadata subtype are now skipped outright, a content stream must actually read as one, every extracted string must be 85% readable characters, and the assembled document must look like prose or the reader reports honestly that it could not read the file. Identity-H PDFs are the reason for that last gate: they contain text, but as glyph indices meaningless without the font's own character map, so the old "this is a scan" message was a small lie and now names both causes. The integrity check meanwhile flagged a completely faithful answer: the model boxed its restatement, oxed survived deLatex with its braces, EQ_CHARS cannot cross a brace, and the restatement became unreadable — so the scan fell through to the next equation, which was "x² + xy + y² = (x+y)² − xy". That is an IDENTITY, true for every x and y: an algebra step, not a claim about the problem. Presentation wrappers are now stripped and leftover braces cleaned up, and any equation that holds everywhere is skipped as a rewrite. Both cases are pinned in the parity corpus, now 516. |
| 2026-08-18 | Integer division was being marked wrong, and the tolerance rule passed one sibling while failing four | Reported from a live answer: a page of correct remainder working came back as five failed checks. "59 ÷ 3 = 19 remainder 2" is right, but the arithmetic match stops at the 19 and the checker judged it as 59/3 = 19 exactly. The tell was in the same screenshot — "59 ÷ 2 = 29" PASSED while its four siblings failed, because 59/2 is 29.5 and an integer written with no decimals earns a ±0.5 tolerance. The checker was contradicting itself in public, which costs more trust than being silent ever would. A quotient equal to the floor of the true value is integer division, and this engine cannot know whether the student meant that or an exact result, so it now says nothing at all. A quotient that is NOT the floor is wrong on both readings and is still reported — but compared exactly, because that same ±0.5 tolerance had been forgiving "59 ÷ 3 = 20" and "7 ÷ 2 = 4", where a wrong quotient is wrong by less than half a unit. An explicit "remainder" following the match settles it outright. Fourteen cases pin the boundary in both engines: silent on integer division, failing on wrong quotients, and still forgiving "1/3 = 0.33" as correct to the decimals written. |
| 2026-08-18 | Question validity ported to the browser, and the parity harness caught doing too little | The validity layer had lived in PHP alone, so /v1/verify could classify an impossible question while the website said nothing — the exact drift the harness exists to prevent, and it read green throughout because no corpus case reached the new code. Porting it was mechanical; proving the port was not. The first sabotage attempt produced no divergence at all, which was the more useful result: the harness compared only the STATE and the number of checks, and both engines can report invalid_question with one check while disagreeing about whether that check PASSED. That is the difference between telling a student their answer was right and telling them it was wrong, and it was invisible. Each verdict now carries a signature of which checks ran and how each landed, and disabling the JS reader for "no solution" wording now fails loudly as question- against question+. Coverage is 532 cases and includes both the impossible question and a solvable control of the same shape, because a false "this question has no answer" is the heaviest wrong claim the system can make and needs a case standing against it permanently. |
| 2026-08-18 | 7Solve synthetic division became a real mathematical layout, not ASCII art | Models lay synthetic division out as hand-aligned monospace, and alignment is the only thing carrying the meaning — which product sits under which coefficient IS the method. Unfenced, every line took the paragraph branch, runs of spaces collapsed to one, and the student got four ragged sentences of loose numbers; fenced, it survived as monospace but read as a lump of code inside a maths answer. The renderer now reads the block and lays it out as a real table where the columns are structural rather than a happy accident of character widths, with tabular figures so the columns stay true at any digit width. **The arithmetic is re-derived from the root and the top coefficient row, and the model own middle and bottom rows must match it** — a block that does not reconcile is left exactly as it came in, as preformatted text. This renderer will not put a wrong computation into a clean table and lend it the authority of good typesetting, which is the failure mode the whole verification layer exists to prevent: it formats what is already right, and it never repairs and never invents. Only two rules are drawn, the vertical one after the divisor and the horizontal one above the result, because those are the two the method actually has; the generic answer-body table rule (100% width, every cell boxed) is wrong here because this is a numeric alignment, not a data grid. Both rules use --ink-3 rather than the --line hairline, by measurement: --line is 1.25:1 on the page surface and almost invisible, --ink-4 was the first pick at 2.36:1 and still under the 3:1 a non-text mark needs, --ink-3 clears at 4.69:1 on the page and 5.07:1 on a white card. No new colour was introduced. Regression coverage is 613 cases, 7 of them the synthetic-division layouts including a wrong-result and a wrong-product block that must both stay preformatted, plus the cubic x^3-6x^2+11x-6 with each of x=1,2,3 verified individually so a substitution regression breaking only one root cannot hide inside a combined answer. One gap is pinned rather than fixed: a cubic answered with only two of its three roots is still reported verified, because every root offered does satisfy the equation and nothing counts them against the degree. |
| 2026-08-18 | 7Solve P0.5: the verifier stopped confusing "every root is genuine" with "these are all the roots" | substitution proved every value offered satisfies the equation and nothing counted them against the degree, so x^3-6x^2+11x-6=0 answered "x = 1, x = 2" was reported verified for the whole life of the product — both roots are real, and the missing 3 is a lost mark the badge said nothing about. `solutionCompleteness` is a Tier 1 mathematical check, in both engines, and it is deliberately **not** a value count: degree is not the number of real roots. It recovers f(x) = L - R by finite differences over x = 0..7, requires the coefficients to be integers, takes rational roots from the rational-root theorem and deflates them exactly by synthetic division, and closes any remaining quadratic in closed form. Anything else is a bail, not a guess — several variables, trig, non-integer coefficients, a residual cubic. (x-2)^2 = 0 verifies from "x = 2" alone because multiplicity two is still one distinct solution value, and "no real solutions" on x^2+1=0 is treated as a COMPLETE answer rather than an empty one. Completeness is asserted over the reals only: where a polynomial also has a complex pair the check refuses to certify the answer complete and says so advisorily, rather than either failing correct real-only work or pretending it is the whole solution set. Two harness defects surfaced while building it. The parity script kept its own hand-written copy of the answer-level kind list — a third copy after the JS and the PHP — so a failing `roots` check made the shipped engine say disputed while the harness said stepfail and blamed PHP; the list is now parsed out of index.html so there is one. And the JS side of the verdict comparison recomposes Checks::run by hand while the PHP side calls run() directly, so a check added to the pipeline and not to that list makes the harness compare two different pipelines and pass — now commented at the point of failure. Coverage is 650 cases and 12 load-bearing negative controls; removing solutionCompleteness makes the incomplete cubic report checked again, by name. |
| 2026-08-18 | 7Solve verification pipeline got a canonical registry; the harness stopped keeping its own copy | The pipeline was described in four places — Verify.run, Checks::run, a hand-recomposed copy inside parity.js, and a hand-written list inside negative-control.js — so a checker added to production and omitted from one of the other three was silent. checks.json is now the single canonical list: parity.js holds it against both shipped pipelines in BOTH directions (a registered check must still be wired; a checker wired into production must be registered, or the suite fails until it is), and negative-control.js drives its removals from it, so registering a checker is what puts it under removal coverage. parity.js now calls each engine production run() rather than recomposing thirteen checkers by hand. registry-proof.js runs the whole loop for real against the shipping files: add a probe to both engines, prove production runs it, prove an UNREGISTERED production check fails the suite, register it and prove the suite accepts it, then restore the tree byte-identical. Making the comparison honest immediately exposed three things the hand-written copy had been hiding. **units() was wired twice in the browser** and once in PHP, so every dimensional warning was printed to the student twice, and the negative control for units was vacuous on the JS side because cutting one of two identical lines leaves the check running — the duplicate is removed. **Fifteen production checkers had never been removal-tested**, including chemistry and extremumCheck; coverage went from 12 to 27, all load-bearing. **The two engines name the not-verified outcome differently** — the browser distinguishes worked/explained/plain/partial so the badge can say what it looked at, while verify.php collapses all four into unverified. Nothing is verified on one side and not the other, but the site and the API do label the same unverified answer differently; that is surfaced in one documented place in the harness rather than hidden, and deliberately not changed in either engine, because renaming in the browser changes the badge and renaming in PHP changes a published API field. Coverage is 677 cases with the registry check, 650 without (the negative control runs with conformance off, so a caught removal means the corpus noticed rather than the registry). |
| 2026-08-18 | 7Solve verification-state contract written down and enforced | The two engines speak different vocabularies and nobody had recorded which words were promises. verify.php states are PUBLIC — /v1 returns them three ways (state verbatim, status uppercased, a verified boolean) and docs/api.html documents them — so checked/disputed/stepfail/invalid_question/unverified are frozen, and `unverified` in particular is preserved for backward compatibility rather than renamed. The browser splits the not-verified outcome four ways (worked/explained/plain/partial) so the badge can say what it looked at; those never reach /v1, but they are NOT free to rename either, because Verify.run state is posted to api.php?action=quality as the `verify` field and the four names are already written into the quality-dashboard history — renaming would silently split historical telemetry and change badge wording a student reads. Everything from either engine maps to exactly three canonical outcomes (verified / disputed / unverified), and stateContract() in parity.js enforces the invariant that no answer is ever verified in one engine and disputed or unverified in the other; the engines may disagree about how to describe a not-verified answer, never about whether the mathematics was proved. An unmapped state is a hard failure rather than defaulting to unverified, since silently defaulting is how a new `verified` synonym would slip past the test. Both failure modes were verified to bite by deliberately breaking the table. VERIFICATION-CONTRACT.md holds the tables and the rule for adding a state. Coverage is 792 cases. |
